<h1>投稿一覧</h1>

<% @lists.each do |list| %>

  <p>タイトル</p>

  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>

 <p>画像</p>
 <% if list.image.present? %>
  <%= image_tag list.image, size: "200×200" %>
<% else %>
  <p>画像がありません</p>
<% end %>

<% end %>
